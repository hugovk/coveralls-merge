language: python

python:
 - pypy3
 - pypy
 - 2.7
 - 3.6
 - 3.5
 - 3.4
 - 3.3

sudo: false

install:
 - pip install pep8 pyflakes

script:
 # Test install
 - python setup.py install

 # Static analysis
 - pyflakes .
 - pep8 --statistics --count .

matrix:
  fast_finish: true
